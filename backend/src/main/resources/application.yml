spring:
  profiles:
    active: mysql
  application:
    name: backend-java
  config:
    import:
      - optional:file:./.env[.properties]
      - optional:file:../.env[.properties]

server:
  port: 8088
  servlet:
    context-path: /
